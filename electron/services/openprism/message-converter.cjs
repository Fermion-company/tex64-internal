/**
 * Message Converter — DEPRECATED.
 *
 * With the migration to AgentExecutor (OpenPrism style), all message format
 * conversion is handled internally by LangChain. This module is kept as a
 * stub so that existing require() calls do not break.
 */

"use strict";

module.exports = {};
