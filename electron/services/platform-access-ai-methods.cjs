/**
 * AI methods mixin for PlatformAccessService.
 *
 * Legacy Gemini conversion utilities have been removed.
 * The agent now uses the OpenAI-compatible run-loop directly
 * (electron/services/openprism/run-loop.cjs).
 */

"use strict";

const aiMethods = {};

module.exports = {
  aiMethods,
};
